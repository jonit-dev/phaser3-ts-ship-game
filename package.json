{
  "name": "getting-started",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "directories": {
    "lib": "lib"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "rm -r ./dist/assets && mkdir ./dist/assets && cp -r ./src/assets/* ./dist/assets && parcel serve index.html",
    "build-assets": "cp -r ./src/assets/* ./dist/assets"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@develephant/types-phaser": "^1.0.1",
    "phaser": "^3.20.1",
    "underscore": "^1.9.1"
  }
}